@model Philosophers.Models.Philosopher

@{
    ViewBag.Title = "Delete";
}

<h2>Are you sure you want to delete @Model.FullName?</h2>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(m => m.DateOfBirth)
        </dt>

        <dd>
            @Html.DisplayFor(m => m.DateOfBirth)
        </dd>

        @if (Model.DateOfDeath.HasValue)
        {
            <dt>
                @Html.DisplayNameFor(m => m.DateOfDeath)
            </dt>

            <dd>
                @Html.DisplayFor(m => m.DateOfDeath)
            </dd>
        }

        <dt>
            @Html.DisplayNameFor(m => m.Area)
        </dt>

        <dd>
            @Html.DisplayFor(m => m.Area.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(m => m.Nationality)
        </dt>

        <dd>
            @Html.DisplayFor(m => m.Nationality.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(m => m.Description)
        </dt>

        <dd>
            @Html.DisplayFor(m => m.Description)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to Philosopher", "Details", new { lastName = Model.LastName })
        </div>
    }
</div>
