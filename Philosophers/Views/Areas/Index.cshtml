@model IEnumerable<Philosophers.Models.Area>

@{
    ViewBag.Title = "Areas";
}

<h2>@ViewBag.Title</h2>

<div>
    @using (Html.BeginForm("Details", "Areas", FormMethod.Get))
    {
        <div>
            Area: @Html.DropDownList("areaName", "Select an area")
            <input type="submit" value="Filter" />
        </div>
    }
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descrpition)
        </th>
        <th>
            Number of philosophers
        </th>
        <th>
            Number of books
        </th>
    </tr>

    @foreach (var area in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(a => area.Name)
            </td>
            <td>
                @Html.DisplayFor(a => area.Descrpition)
            </td>
            <td>
                @area.Philosophers.Count
            </td>
            <td>
                @area.Books.Count
            </td>
        </tr>
    }

</table>
